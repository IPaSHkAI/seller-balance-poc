name: 'seller_balance'
version: '1.0.0'
config-version: 2

profile: 'seller_balance'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  seller_balance:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: dwh

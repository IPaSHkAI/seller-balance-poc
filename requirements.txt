# Зависимости для локальной разработки/тестов (генератор данных, DQ-модуль, pytest).
# Зависимости самого Airflow-контейнера — в airflow/requirements-airflow.txt.
pandas==2.2.2
pytest==8.2.2
ruff==0.5.0
